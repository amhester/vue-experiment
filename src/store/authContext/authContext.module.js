'use strict';

const state     = require('./authContext.state');
const mutations = require('./authContext.mutations');
const actions   = require('./authContext.actions');

module.exports = {
    state,
    mutations,
    actions
};