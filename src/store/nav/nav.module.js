'use strict';

const state     = require('./nav.state');
const mutations = require('./nav.mutations');
const actions   = require('./nav.actions');

module.exports = {
    state,
    mutations,
    actions
};